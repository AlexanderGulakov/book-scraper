requests>=2.31,<3
PyYAML>=6.0,<7
beautifulsoup4>=4.12,<5
lxml>=6.0
# Прикидається справжнім Chrome на рівні TLS — без цього OLX віддає 403
# на будь-який запит з дата-центру. Якщо колесо не ставиться на вашу
# платформу, код тихо відкотиться на requests.
curl_cffi>=0.7,<1
